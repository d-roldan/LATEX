services:
  disal-nginx:
    ports:
      - "0.0.0.0:${HTTPS_PORT:-443}:443"
    volumes:
      - ./infra/nginx/https.conf:/etc/nginx/conf.d/default.conf:ro
      - ./infra/nginx/certs:/etc/nginx/certs:ro
